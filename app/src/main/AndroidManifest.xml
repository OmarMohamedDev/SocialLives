<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="omar.mohamed.socialphotoneighbour">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!-- External storage for caching. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- My Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup= "true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar" >

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCMKxH_EcA4zKJOGKpyAs7fBDCUjk0y0oE" />

        <service
            android:name="omar.mohamed.socialphotoneighbour.services.BackgroundService"
            android:exported="false" />

        <activity

            android:name="omar.mohamed.socialphotoneighbour.activities.ItemListActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="omar.mohamed.socialphotoneighbour.activities.ItemDetailActivity"
            android:label="@string/title_item_detail">
            <!--<meta-data-->
                <!--android:name="android.support.PARENT_ACTIVITY"-->
                <!--android:value=".activities.ItemListActivity" />-->
            <!-- TODO: Put here temporary until fix for menu and map -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- -->
        </activity>
        <activity
            android:name="omar.mohamed.socialphotoneighbour.activities.FullScreenActivity"
            android:label="@string/title_full_screen">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.ItemDetailActivity" />
        </activity>
    </application>

</manifest>